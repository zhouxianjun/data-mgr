<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alone.core.mapper.RoleMapper">
    <select id="listByUser" resultType="Role">
        SELECT
        r.*
        FROM
        t_role r
        LEFT JOIN t_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{user}
    </select>

    <update id="updateStatus">
        UPDATE t_role SET `status` = #{status} WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateChildStatus">
        UPDATE t_role SET `status` = #{status} WHERE FIND_IN_SET(#{pid}, pids)
    </update>

    <select id="getSameIncludeMenuOfExcludeSelf" resultType="long">
        SELECT
          rm.menu_id
        FROM
          t_role_menu rm
        LEFT JOIN t_user_role ur ON ur.role_id = rm.role_id
        WHERE
          ur.user_id = #{user}
        AND ur.role_id != #{role}
        AND FIND_IN_SET(rm.menu_id, #{include})
        GROUP BY rm.menu_id
    </select>

    <delete id="deleteRoleChildrenMenus">
        DELETE rm.*
        FROM
          t_role_menu rm
        LEFT JOIN t_user_role ur ON ur.role_id = rm.role_id
        LEFT JOIN t_user u ON u.id = ur.user_id
        WHERE
          ur.role_id = #{role}
        AND FIND_IN_SET(#{user}, u.pids)
        AND FIND_IN_SET(rm.menu_id,#{menus})
    </delete>
</mapper>